1旨在拓展object的属性

2如何添加属性 scala用mix一词 颇能中其肯綮

3模式的大貌

给主对象添加一个容器 能够容纳不同属性 这里相当于setter
然后混入相应的interface 该接口只提供getter方法 然后就能构成完整的bean了

简言之 这就跟数据库的主从集群类型  一处写 多出读

4可以应用的地方
a 统一配置中心 可以用此来维护 用redis mongoDb 来存储 做分布式写/读
